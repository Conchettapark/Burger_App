<h1>Eat Da Burger!</h1>

<h2>Burgers!</h2>

   <div>
      <h2>Burgers to Eat</h2>
      <ol>
        {{#each burgers}}
          {{#unless devoured}}
            <li>{{this.burger_name}}  <button class="devour" data-id={{this.id}}> Devour</button></li>
            {{!-- {{ cats/cat-block sleep=true}} --}}
          {{/unless}}
        {{/each}}
      </ol>
    </div>

      <div class="col-sm-3 delete-burg">
      <h2>Burgers Devoured</h2>
        <ol>
          {{#each burgers}}
            {{#if devoured}}
            <li>{{this.burger_name}}  <button class="delete" data-id={{this.id}}> Trash</button></li>
              {{!-- {{> cats/cat-block sleep=false}} --}}
            {{/if}}
          {{/each}}
        </ol>
    </div>
{{!-- <ul>
  {{#each cats}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul> --}}

        {{!-- {{#each burgers}}
          {{#unless devoured}}
          <form action="burger/eat/{{id}}" method="POST">
            <div class="input-group">
              <li class="form-control">{{id}}. {{burger_name}}</li>
                <span class="input-group-btn">
                  <button class="btn btn-success devour-button" type="submit">Devour It!</button>
                </span>
            </div>
          </form>
          {{/unless}}
        {{/each}} --}}

{{!-- <h2>Cats that are sleepy!</h2>

<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul> --}}

 <h2>Add A Burger</h2>
    <form class="create-form">
      <div class="form-group">
        <label for="ca">Burger:</label>
        <input type="text" id="ca" name="name">
      </div>
      <div class="form-group">
        {{!-- <label for="slee">Devour?</label><br> --}}
         <button type="submit">Add Burger</button>
        {{!-- <input type="radio" name="devour" value="0" checked hidden> <br>
        <div>What burger do you want?</div>
        {{!-- <input type="radio" name="devour" value="1"> Devoured --}}
     

</form>
